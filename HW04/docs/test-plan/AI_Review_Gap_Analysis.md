# AI review & gap analysis — what the AI got wrong in the generated scripts

This file is the evidence for the "Review and fix (human review)" bullet of Task 1.
The brief asks for two things per finding, and the second is where the marks are:
**what** the AI got wrong or missed, and **why** it missed it (prompt quality,
model limitation, or a characteristic of the feature).

Fill one row per correction you actually made. A row you cannot point to in a diff
does not belong here.

## Corrections log

Findings 1–6 come from the review of `fr01.registration.spec.ts` at Stage 5, before the
suite was ever executed. Categories follow the brief's own list, named in the last column.

| # | Feature | File / line | What the AI produced | Why it is wrong | What I changed it to | Why the AI missed it |
|---|---|---|---|---|---|---|
| 1 | FR-01 | `fr01.registration.spec.ts`, redirect branch | `await expect(registerPage.errorBanner).toBeHidden()` after the app navigates to `/login` | **Weak assertion — unfalsifiable.** `div.bg-red-100` is rendered only by `/register`, so once the URL has changed the element cannot exist on any page. The assertion is green no matter how the app behaves, including if registration is completely broken. | `await expect(registerPage.submitButton).toBeHidden()` — if the app had stayed on the form and rendered an error, the "Đăng Ký" button would still be on screen, so this statement can actually fail. | The line was written for the `native` branch, where the form is still on screen and "no app error was shown" is a real claim, then reused in the success branch by symmetry. The AI carried the *wording* of the check across without re-checking its *precondition* — after a redirect, "hidden" and "does not exist" are indistinguishable. Pattern-matching on shape rather than reasoning about what could make the assertion red. |
| 2 | FR-01 | `fr01.registration.spec.ts`, channel dispatch | `if (native) … if (banner) … ` with the success path as the implicit fall-through, and `FIELD_KEYWORD[row.expect_signal]` used unguarded | **Wrong-by-construction.** A typo in the data file (`redriect`, or an `expect_signal` that is not a field name) would silently select the success path for a case written to be *refused* — and that case could then pass. The unguarded lookup would also crash with `undefined` rather than say what was wrong. | `switch` with an explicit `default` that throws naming the bad value, plus a `keywordFor()` guard that fails with `"<tc_id>: expect_signal … is not a known field - fix the data file"`. | The AI wrote the branches against the four values that were in the CSV at that moment, treating the data file as a fixed input. But the data file is the artefact that changes most often in a data-driven suite — it is edited every time a case is added. Defensive handling of *its* errors was never considered, because the AI was thinking of the data as trusted rather than as the most volatile input. |
| 3 | FR-01 | `fr01.registration.spec.ts`, banner branch | Hard `expect` on the error banner, placed before the check that no account was created | Not a false pass, but **it destroys the evidence that matters most**. The SUT performs no server-side validation, so most banner rows will fail at the very first assertion and abort — the report will say "no banner shown" and never reveal the more serious fact that the refused registration *created the account anyway*. | The three UI statements became `expect.soft`, so the data-integrity probe still runs and both facts land in the report. Soft failures still fail the test. | The AI ordered the assertions the way a person reads the page (UI first, database second) instead of the way a triage session needs them. It optimised for narrative order, not for what a failing run must still be able to tell you. Fail-fast is the right default; recognising the case where it discards the diagnostic requires thinking about the *failing* run, which the AI did not do. |
| 4 | FR-01 | `fr01.registration.spec.ts`, api branch | The four API cases asserted the HTTP status only | **Weak assertion.** A status code is a claim, not a fact. A server that answers `400` and inserts the row anyway, or answers `200` and inserts nothing, passes on status alone — and this backend (`backend/server.js:38-48`) does no validation at all, which is exactly the class of defect these rows exist to catch. | Added a pattern-3 probe: a success row must be able to log in and return the submitted name and e-mail; a rejected row must not be usable. Status assertion made soft so it cannot abort before the probe. | The AI had internalised the brief's "three distinct assertion patterns" as a *taxonomy assigned per branch* — UI rows get pattern 1, API rows get pattern 2 — instead of as a minimum to reach across the suite. Once a branch was labelled "the contract one", it stopped asking what else was observable there. |
| 5 | FR-01 | `fr01_registration.csv`, TC-20 | E-mail literal `user+tag@mail.domain.com` | **Wrong-by-construction data.** The only success case with a constant address. The SUT database is never reset between the nine required runs, so from run 2 the row no longer tests plus-addressing on a fresh account — it tests it on an account a previous run left behind. | `user+tag{{RUN}}@mail.domain.com`, with the spec resolving `{{RUN}}` to a per-run identifier. Expected result unchanged. | The AI wrote the data file one row at a time against the *design intent* of each case, so "the address must show plus-addressing" was satisfied and it moved on. Repeat-execution is a property of the suite as a whole, not of any single row, so a row-by-row author never encounters it. Note the first stated reason for this fix ("it would collide on a duplicate e-mail") was itself wrong and was corrected after reading the schema — see finding 6. |
| 6 | FR-01 | `automation/data`, cross-run assumption | Assumed a repeated e-mail would be rejected by the database, and reasoned about re-runs on that basis | **Unverified premise.** `backend/database.js:50-61` shows `users.email` is plain `TEXT` with **no UNIQUE constraint**. Duplicate registrations do not error; they insert another row. So no assertion actually flips between run 1 and run 2 — but the database accumulates rows monotonically across the nine runs, which will distort FR-13's dashboard figures. | No spec change needed; the assumption was replaced with the schema fact and the accumulation risk is now a known input to FR-13. | The AI reasoned from what a registration system *conventionally* does rather than from this system's schema, and stated the conclusion with more confidence than the evidence supported. This is the same failure mode the whole project rule "the running SUT outranks the documents" exists to prevent — here the "document" was the AI's own prior about how e-mail columns are normally defined. |

Finding 7 comes from triaging the first executed run (2026-08-10), not from a pre-run review.

| # | Feature | File / line | What the AI produced | Why it is wrong | What I changed it to | Why the AI missed it |
|---|---|---|---|---|---|---|
| 7 | FR-01 | `fr01_registration.csv`, TC-01b `expect_signal` | Expected status **`201`** for a successful `POST /api/register` | **Wrong expected value — the test was red, the build was right.** `api_specification.md` §1.1 documents the success response of this endpoint as **`200 OK`** with `{"message": …, "id": n}`. The run returned 200, i.e. exactly what the SUT's own contract specifies, so the red came from the test, not from the build. Left as it was, this row would have counted a conforming endpoint as a divergence and could have ended up in the bug report. | `expect_signal` = **`200`**, note updated to record that the status was confirmed against `api_specification.md` §1.1. The body assertion is untouched: a success row must still come back with a numeric `id`, and must still be usable via `/api/login` with the submitted name and e-mail. | The AI filled the cell from REST convention — "creation returns 201" — instead of from this SUT's API document, which was sitting in the repo and answered the question outright. It did register the uncertainty, writing *"status to confirm on first run"* in the note column, but still emitted a concrete expectation rather than stopping to read the one file that would settle it; a flagged guess reads as diligence while behaving exactly like an unflagged guess. Same root cause as finding 6: the AI's general prior about how systems normally behave was allowed to outrank a document describing this system. |

Findings 8–11 come from FR-11's Stage 3 (locator verification), before the data file or
spec were written. All four were in the AI's own scaffold — `OrderHistoryPage.ts` and
`LoginPage.ts` as first generated — and all four were found by running a throwaway probe
that printed `count()` for every candidate locator, not by re-reading the code. Findings 10
and 11 are the two that matter most for the report: both are **silent** failures that a
green suite would have hidden.

| # | Feature | File / line | What the AI produced | Why it is wrong | What I changed it to | Why the AI missed it |
|---|---|---|---|---|---|---|
| 8 | FR-11 | `OrderHistoryPage.ts`, `path` | `readonly path = '/orders'` | **Route does not exist.** `frontend-web/src/App.jsx` declares eight routes and `/orders` is not among them; the order-history table is a panel inside `Profile.jsx`, served at `/profile`. Every one of the 13 UI cases would have navigated to a blank page and failed identically — the pitfall the skill records as "every case fails identically", which reads as a broken suite rather than a one-word config error. | `readonly path = '/profile'`, with the doc comment stating the fact and why HW02 got it wrong too. | The AI named the page object's path after the **feature** ("order history" → `/orders`) rather than reading the router. This is the same mistake HW02 made when it concluded FR-11 "has no web UI" — it searched for a route by name instead of for the rendered feature — which means the AI reproduced a documented error that was already written down in `TC_Matrix_FR11.md`, in the very file it was scaffolding against. A convention-shaped guess was preferred over one `grep` of `App.jsx`. |
| 9 | FR-11 | `LoginPage.ts`, all three getters | `getByLabel(/email/i)`, `getByLabel(/password\|mật khẩu/i)`, `getByRole('button', {name:/login\|đăng nhập/i})` | **All three resolve to 0 elements.** Probe counts: 0, 0, 0. In `Login.jsx` every `<label>` lacks `htmlFor` and every `<input>` lacks id, name, placeholder and aria-label, so `getByLabel` cannot bind either field; the submit button's literal text is **"Sign In"**, which neither `/login/i` nor `/đăng nhập/i` matches. `getByRole('textbox')` returns 2 with no accessible name to separate them. | Label-text-anchored CSS, `form div:has(> label:text-is("Username")) input` → 1 each, and `getByRole('button', {name:'Sign In'})` → 1. Named `emailInput` even though the label says "Username", because `Login.jsx` binds it to the `email` state and posts it as `email`. | Two distinct causes. (a) The AI wrote locators from the shape `getByLabel` *documentation* implies — that a visible label binds to its input — without checking that this app wires them up; it had already learned exactly this lesson on `RegisterPage.ts`, but the scaffold for all three features was generated in one pass before any page was read, so the correction never propagated sideways to `LoginPage`. (b) The button regex was hedged bilingually, `/login\|đăng nhập/i`, which *looks* like it accounts for a Vietnamese UI — but this SUT mixes languages per element, and the one string the AI guessed around was English. Hedging across two languages still misses a third possibility, and a hedge that misses is indistinguishable from a guess. |
| 10 | FR-11 | `OrderHistoryPage.ts`, `columnValues()` | `return this.rows.locator('td').nth(columnIndex).allInnerTexts()` | **Silently returns one value instead of a column.** Playwright resolves `rows.locator('td')` to *all* cells across *all* rows flattened into one list, so `.nth(1)` is the second cell of the first row — not the second cell of each row. Probed against a 16-row table: the scaffold form returned **1** value, `["#16"]`; the correct form returned **16**. Any sortedness or column assertion built on it would have compared a one-element array, which is trivially sorted — so `expectSortedDescByDate` would have **passed on a table sorted in any order at all**. An unfalsifiable assertion, like finding 1, but arrived at through a locator-semantics error rather than a precondition error. | `this.rows.locator('td:nth-child(${column})')` with a 1-based `column` argument, plus `orderIds()` which parses `"#16"` → `16`. The doc comment records the 1-vs-16 count so the reason survives. | `rows.locator('td').nth(1)` reads in English as "the 1st cell of each row" and that is what the AI meant; Playwright's actual semantics are "the 1st element of the flattened set". The code is syntactically valid, type-checks, and looks right on review — nothing distinguishes the two readings **except executing it and counting**. This is precisely the class of bug the skill's "require exactly 1" probe rule exists for, and it is the strongest argument in this project that reading generated code is not a substitute for running a probe against it. |
| 11 | FR-11 | `OrderHistoryPage.ts` / `fr11.order-history.spec.ts`, row lookup | Row lookup by `filter({ hasText: '#' + orderId })` | **Latent false pass that only appears after repeated runs.** `hasText` is a substring match, so `'#1'` also matches `#10`–`#19`. With single-digit ids it resolves to 1 row and looks correct; probed once ids passed 9, `filter({hasText:'#1'})` matched **8** rows. Order ids climb monotonically because the SUT database is never reset between the nine required runs, so this is guaranteed to trigger — and it triggers as an *ambiguous locator*, i.e. it can silently assert against the wrong order's status badge rather than erroring. | `filter({ has: page.getByText('#' + orderId, { exact: true }) })` → 1 row at any id width. | The AI validated the locator mentally against the fixture it was imagining — a handful of orders with small ids — rather than against the state the suite reaches on run 6 of 9. Same root cause as findings 5 and 6: **repeat-execution is a property of the suite as a whole, invisible to anyone authoring one row or one locator at a time.** Three separate findings now share this cause, which suggests the fix is a review question asked of every locator and every data row ("what does this look like on run 9?") rather than three individual corrections. |


Findings 12–17 come from FR-13's Stage 3 (locator verification) on 2026-08-10, before the
data file or spec were written. All six were in the AI's own scaffold — `AdminDashboardPage.ts`
and `fr13.dashboard.spec.ts` — and every count below was probed on **all three engines**, which
returned identical numbers.

| # | Feature | File / line | What the AI produced | Why it is wrong | What I changed it to | Why the AI missed it |
|---|---|---|---|---|---|---|
| 12 | FR-13 | `AdminDashboardPage.ts`, `path` | `readonly path = '/admin/dashboard'` | **Route does not exist, and the origin is wrong twice over.** `frontend-admin/src/App.jsx` imports no router; the section is chosen by a `useState('dashboard')`, so the dashboard is the landing view at the app root and `/admin/dashboard` is a 404. Worse, `BasePage.goto()` resolves a relative path against `playwright.config.ts`'s `baseURL`, which is the **customer** app on `:5173` — so the scaffold would have navigated to `localhost:5173/admin/dashboard`: wrong path *on the wrong application*. Both failures present as "every case fails identically", the exact symptom the skill's Step 0 pitfall table warns reads like a broken suite rather than a config error. | `readonly path = process.env.ADMIN_URL ?? 'http://localhost:5174'` — absolute, so `page.goto()` ignores `baseURL`. The doc comment states why. | Same root cause as finding 8, reproduced one feature later: the AI named the path after the feature (`/admin/dashboard` is what an admin dashboard is *called*) instead of reading the router — and here there was no router to read, which is itself the finding. The origin half is new: the AI treated `baseURL` as "the SUT" when this SUT is three applications on three ports, so a page object for the admin app cannot inherit the customer app's base. |
| 13 | FR-13 | `AdminDashboardPage.ts`, `kpiCard()` / `revenueValue` | `getByRole('region', {name}).or(getByTestId('kpi-${name}')).first()`, then `.getByTestId('kpi-value')` | **Every branch matches nothing.** Probed on the live dashboard: `getByRole('region')` → **0**, `getByTestId('kpi-value')` → **0**. A KPI card is `<div class="bg-white p-6 rounded shadow"><h3>label</h3><p>value</p></div>` — no landmark role, no accessible name, no test id anywhere in the admin app. The `.or().first()` construction makes this worse than a plain miss: it reads as defensive breadth, but `.first()` on an empty union still yields an empty locator, so the fallback chain manufactures confidence without adding a single real candidate. | The label is addressed by its accessible name — `getByRole('heading', {name: 'Tổng doanh thu (Delivered)'})` → **1** — and the value by stepping one sibling from it, `xpath=following-sibling::p[1]` → **1**, with the mandatory comment saying why nothing better existed. | The AI wrote locators for the dashboard it expected a well-built admin app to have (landmarks and test ids) rather than for the one in the repo, and then hedged with `.or()` instead of looking. Hedging is what the scaffold does *in place of* checking: two guesses joined by `.or()` still equal zero matches, and only a count exposes that. |
| 14 | FR-13 | `AdminDashboardPage.ts`, three getters | `userCountValue`, `dateRangeFilter`, `recentOrdersTable` | **Getters for controls the page does not have.** Structural counts on the dashboard tab: headings matching `/người dùng/i` in the main panel → **0** (the sidebar tab of that name is not a KPI), `main select, main input` → **0**, `table` → **0**. The dashboard renders exactly **two** cards, revenue and order count. The consequence is not just three dead getters: `fr13_dashboard.json` already carried `date_range: "last_30_days"` on TC-02, a case that cannot exist on this build. | All three getters deleted rather than left dead, with the counts recorded in the doc comment. `date_range` must come out of the data file at Stage 4, and any date-filter case moves to the "could not be automated" list. | The AI derived the getters from the *feature name* — a dashboard has KPIs, a date filter and a recent-orders table — which is a description of the category, not of this build. This is the "cases written for a field that does not exist" pitfall, and it is why the skill orders locator verification **before** the data file: had the data file been written first, TC-02 would have been authored, coded and only then discovered to be unrunnable. |
| 15 | FR-13 | `fr13.dashboard.spec.ts`, cross-check branch | `sum + Number(order.total ?? 0)` over `GET /api/admin/orders` | **Silently computes zero.** The orders payload keys the amount as **`total_amount`**; there is no `total` field, so `?? 0` — written as a safety net — converts a missing field into a valid-looking total. Probed against the live API: `SUM(total)` = **0** vs `SUM(total_amount)` = **4,000,000**. The expected value would have been 0 for every run, and because the page never renders 0 the case would have failed with a *misleading* message ("expected 0, got 8,000,000"), pointing triage at the UI when the defect was in the test's own arithmetic. | Read `total_amount`; the page object exposes `gotoAndLoadDashboard()`, which returns the same `/api/admin/orders` payload the UI derived its figure from, so the comparison is against the actual source rather than a re-fetch. | The AI guessed the field name from convention (`total`) and then *insulated the guess* with `?? 0`. The nullish default is the real error: it converts "I was wrong about the schema" into a plausible number instead of a crash. A defensive default on a value that is the entire point of the assertion removes the only signal that would have caught the mistake. |
| 16 | FR-13 | `fr13.dashboard.spec.ts`, API cross-check | `request.get('${API_URL}/api/admin/orders')` with no headers | **Unauthenticated — 401, not data.** `/api/admin/orders` is behind `authenticateToken`; probed: **401** with no token, **200** with a Bearer token. The request would never have returned orders, so the cross-check that exists specifically to catch the revenue defect could not run at all. | The spec must send an admin Bearer token (obtained from `POST /api/login`) on this call. Recorded here at Stage 3; the correction itself lands in the spec at Stage 5. | The AI used Playwright's `request` fixture as though it inherited the browser's session. It does not — it is an independent context with no cookies and no headers from the page. The scaffold also had no need to think about auth, because it had already assumed a UI login would carry it. |
| 17 | FR-13 | `fr13.dashboard.spec.ts`, `forbidden` branch | `await expect(page).toHaveURL(/login\|403\|unauthorized/)` for the non-admin case | **Cannot hold, and hides the more serious fact.** The admin app is a single URL: on a refused login it stays on `http://localhost:5174/` and reports the refusal through `alert("Bạn không phải là admin!")` — probed on all three engines, URL unchanged, `adminToken` null. Playwright auto-dismisses dialogs when no handler is registered, so the scaffold would have seen only "still on the login form" and could not say why. Separately, the branch asserts access control **at the UI only**, and the gate is client-side: `handleLogin` checks `res.data.user.role !== "admin"` in the browser, while `authenticateToken` (`server.js:118-128`) verifies the JWT and never reads `role`. Probed with a `test@eshop.com` token: `GET /api/admin/orders` → **200**, `GET /api/admin/users` → **200**. A UI-only case therefore reports the gate as working. | `attemptLogin()` installs a dialog handler and returns the captured messages plus whether the dashboard was reached, so the negative cases assert on the app's own string. FR-13 needs the same check at the API layer or it cannot see past the client-side gate. | Two causes. The URL assertion is finding 8's pattern again — the AI assumed refusal is expressed as navigation, the default for a routed app, and this app has no routes. The access-control half is more interesting: the AI tested the *mechanism it could see in the UI* and treated a passing UI check as evidence the rule is enforced. Enforcement lives at the tier the UI cannot vouch for, and nothing in a UI-only suite will ever reveal that. |

The three raw observations behind findings 15 and 17 — rendered revenue `8,000,000 ₫` against a
delivered-order total of `4,000,000`, and the two `200`s for a non-admin token — are actual-vs-expected
evidence, not verdicts. Whether either is a defect is the student's call and belongs in
`docs/02_Bug_Report.md`, not here.

Findings 18–25 come from the **student's review of the FR-13 spec and data file** on
2026-08-10, at the Stage-5 gate and before any execution. This is the first block in this
file found by the human reviewer rather than by the AI, and the pattern across it is worth
naming: every one is a place where the AI wrote an assertion that *looks* like a check but
cannot fail, or moved a decision out of the data file and into code. None would have been
caught by `tsc`, and none by `--list`.

| # | Feature | File / line | What the AI produced | Why it is wrong | What I changed it to | Why the AI missed it |
|---|---|---|---|---|---|---|
| 18 | FR-13 | `fr13_dashboard.json`, TC-13b | `expect_revenue_delta: 0` for an order walked `canceled → delivered`, on the reasoning that TC-13 says the transition should have been refused | **The expectation contradicts the requirement the case belongs to.** README FR-13 L183 defines revenue over `status = 'delivered'` with no clause about how the order reached that status. Once the order *is* delivered, FR-13 requires it to be counted — so expecting a delta of 0 encodes **TC-13's** rule (the transition is illegal) into an **FR-13** row. Both cases would then report the same underlying defect, and TC-13b would fail even against a build whose revenue logic was perfect. | `expect_revenue_delta: 777000`. The row now tests what it was always for — that the dashboard *recomputes* after a transition rather than caching — and the transition's legality stays with TC-13 at the API. | The AI reasoned "the order should never have been delivered, therefore revenue should not move", chaining one requirement's conclusion into another requirement's expectation. Each case must be evaluated against the *state the system is actually in* at assertion time, not against the state it would be in if a different case's rule had been honoured. This is the same class as finding 1: an assertion that cannot be red for the reason the case claims to test. |
| 19 | FR-13 | `fr13.dashboard.spec.ts`, `deliveredTotal()` | `sum + Number(order.total_amount ?? 0)` | **Finding 15 reintroduced by the AI in its own replacement code.** The function exists to be an *independent* source of truth for the revenue expectation; a nullish default converts "the field I am reading does not exist" into a plausible `0`, so a schema change would silently make the expected value 0 for every row and the failure message would blame the UI. | Throws naming the order id and the offending value, with an explicit instruction not to default it to 0. | The AI copied the defensive idiom from the scaffold it was *replacing* — the very line it had logged as finding 15 four hours earlier. Writing the finding down did not prevent repeating it, because `?? 0` is reached for reflexively while the attention is on the surrounding logic. That is an argument for a lint rule over a log entry. |
| 20 | FR-13 | `fr13.dashboard.spec.ts`, `revenue_format` | `expect(rendered).toMatch(/\d(?:[., \s]\d{3})*/)` | **Unanchored — the assertion cannot fail.** Without `^…$` the pattern matches any string containing a single digit. Verified: `"abc7xyz"`, `"8000000"` (ungrouped) and `"8,00,000"` (misgrouped) all pass the old form and all fail the new one. The case advertises "revenue is formatted as currency" and actually asserts "contains a digit". | `^-?\d{1,3}(,\d{3})*(\.\d+)?$` applied to the numeric token after stripping `₫`, with a comparison table of accepted and rejected samples kept as evidence. | The AI wrote the regex to *describe* a grouped number and never asked what else it would accept. `toMatch` takes a search, not a match, and that distinction is invisible when the sample you test against is a well-formed value. Every regex assertion needs a deliberately malformed sample before it is trusted. |
| 21 | FR-13 | `fr13.dashboard.spec.ts`, `ui_login_refused` | `if (row.tc_id === 'FR13-TC-17') { … }` | **Breaks the data-driven contract from the inside.** With behaviour keyed off an identifier, the data file stops being the specification of the cases: a new unknown-email row would silently take the wrong branch, and renaming a `tc_id` would change what a case tests. The suite reads as data-driven and behaves as hard-coded. | A `credential` column (`valid` / `wrong_password` / `unknown_email` / `empty`) that the switch dispatches on. No branch in the spec now references a `tc_id`. | The AI reached for the identifier because it was the shortest way to express a one-off, and treated "one case needs this" as licence to special-case rather than as a missing column. The rule the brief actually imposes is that *every* input lives in the data file — a single exception is the whole violation. |
| 22 | FR-13 | `fr13.dashboard.spec.ts`, TC-11 | `expect(Number(stored.total_amount)).not.toBe(submitted)` | **Weak assertion — passes on almost any wrong answer.** README FR-08 requires the total to be *recomputed from the cart*. A not-equal check is satisfied by any figure that differs from the submitted one, including a clamp to 0, an off-by-one, or a garbage value — it verifies that the build did *something*, not that it did the right thing. | The cart is now populated through `POST /api/cart` with known prices, so the correct total is exactly `150000*2 + 99000 = 399000`, and the row asserts that value. | The AI wrote the assertion the *defect* suggested ("the submitted number must not survive") instead of the one the *requirement* states ("the stored number must equal the cart total"). Negative-shaped assertions are seductive because they are easy to satisfy from the failing build's perspective; they are also the ones most likely to stay green after a partial fix. |
| 23 | FR-13 | `fr13.dashboard.spec.ts`, throwaway identities | `fr13-${tag}-${RUN_ID}@eshop.test`, with `tag` shared across cases (`seed`, `write`, …) | **Silent cross-case coupling.** `users.email` has no UNIQUE constraint (finding 6), so a repeated address inserts a second row while `SELECT * FROM users WHERE email = ?` keeps returning the *first*. Several cases would therefore transact as one identity, and orders seeded by one case would be owned by another's user — invisible until a case failed for a reason belonging to a different case. | The e-mail carries the `tc_id`: `fr13-${slug}-${RUN_ID}@eshop.test`, unique per case *and* per run. | The AI made the address unique per *run* and stopped, because the failure it had in mind was collision with a previous run. Uniqueness within a run needed the schema fact from finding 6 to be applied a second time, in a different place — knowing a fact and applying it everywhere it bears are separate acts. |
| 24 | FR-13 | `fr13.dashboard.spec.ts`, delta assertions | `toBeCloseTo(expected, 2)` for every delta, integer or not | **One tolerance for two different kinds of value.** Precision 2 accepts anything within ±0.005, so an integer delta of `6000` would pass at `6000.004` — a real tolerance where none is needed — while for the sub-unit rows it says nothing about *why* a tolerance exists. | Integers are compared **exactly** (`toBe`); only the two sub-unit rows carry `delta_precision: 3`, which is the ceiling the UI imposes (`toLocaleString()` renders at most 3 fraction digits), documented per row. The precision is now a data column, so a reviewer can see it. | The AI reached for a single "safe" tolerance to cover a float it was uneasy about, rather than separating the two cases: an integer delta needs no tolerance, and a decimal one needs a *justified* number tied to the display. A tolerance chosen for comfort is indistinguishable from one chosen to make a case pass. |
| 25 | FR-13 | `fr13_dashboard.json` (whole file) | No runtime validation; `_meta.case_count` maintained by hand | **The data file is trusted while being the most volatile artefact.** `tsc` cannot check it (it is parsed at runtime) and `--list` will happily enumerate rows with a duplicate `tc_id`, an unknown `check` or a non-numeric amount. Proven in practice: after adding five cases, `_meta.case_count` still read `35` against 40 rows. | `validateCases()` runs at collection and throws with **all** problems at once — duplicate ids, the type/layer/actor/check/status enums, finite amounts, positive-integer `repeat`, non-empty integer status lists, `delta_precision`, cart shape, and the declared count. Verified by injecting four defects and confirming all four were reported. | Finding 2 established that the data file is the most frequently edited input and must be treated as untrusted; the AI applied that to *dispatch* (guarding the `check` switch) but not to the *file as a whole*. Guarding the branch you are writing is local and obvious; validating the artefact is a separate step that nothing in the immediate task prompts you to take. |

Findings 26–33 come from the **student's review of FR-11 Stage 5**, which withheld
approval to run the suite. Like findings 18–25 they were found by the human reviewer
reading the generated spec against the requirement, not by the AI probing its own output —
and unlike findings 8–11, none of them would have been caught by `tsc` or by `--list`.
Two of them (26, 27) would each have invalidated the whole FR-11 run.

They are numbered from 26 because findings 12–25 were written for FR-13 in a parallel
session while this block was being drafted; this block originally used 12–17 and was
renumbered rather than allowed to collide. The cross-references in `env.ts`,
`fr11.order-history.spec.ts` and `TC_Matrix_FR11.md` were updated to match.

| # | Feature | File / line | What the AI produced | Why it is wrong | What I changed it to | Why the AI missed it |
|---|---|---|---|---|---|---|
| 26 | FR-11 | `playwright.config.ts`, `fr11.order-history.spec.ts`, `run-all-browsers.ps1` | Credentials read as `process.env.USER_EMAIL ?? ''` etc., with **nothing anywhere loading `.env`** | **Blocker — the whole feature could not run.** `dotenv` was not a dependency, was not in `node_modules`, and appeared in no file; `node -e` confirmed USER_EMAIL, USER_PASSWORD, ADMIN_EMAIL and ADMIN_PASSWORD were all absent from a normal shell. So every FR-11 case would have stopped at its own credential guard. Worse, the evidence I had presented as proof of health — `--list` reporting 40 tests — **cannot detect this**, because collection never runs a hook. I had verified the suite with the one check that was structurally blind to the defect. | Added `dotenv` as a declared devDependency and created `automation/utils/env.ts`, which loads `.env` at module scope and exports the values, `envReadiness()` (booleans only) and `assertCredentialsPresent()`. `student.ts` and the config import it **first**; the spec and `eshop-api.ts` take their values from it instead of reading `process.env`. `envReadiness()` is spread into the report `metadata`, so every HTML report now carries boolean proof that `.env` was loaded, with no secret in it. Verified: all six flags `true`, `missingCredentials()` empty. | Two compounding causes. (a) The AI wrote `process.env.X ?? 'default'` for every variable, and that idiom *looks* complete — the `??` reads as "this is handled". For URLs the fallback was genuinely correct, so the pattern was validated four times before being applied to credentials, where a fallback of `''` is not a default but a silent failure. (b) It then trusted the wrong verification. It had a real ordering hazard to reason about (ES imports are evaluated before the importing module's statements, so a `loadDotenv()` call in the config body runs too late) and never reached it, because `--list` was green and the AI accepted a collection-time check as evidence about run-time behaviour. **A test suite's own tooling can produce false confidence, and this is the clearest example of it in the project.** |
| 27 | FR-11 | `fr11_order_history.json`, TC-11 and TC-14 | `expect_cancel_visible: true` for a `shipping` order, and `expect_cancel_count: 3`, alongside TC-26/TC-27 asserting that a `shipping` order must **not** be cancellable | **Blocker — contradictory oracles, and the suite defends the defect.** The same file asserted both that the shipping cancel button must be present (TC-11, TC-14, both `positive`) and that shipping cancellation must be refused (TC-26, TC-27). Those cannot both be the requirement. Because TC-11/TC-14 were calibrated to the current build, **a SUT fixed in the obvious way — hiding the button once an order ships — would have turned two positive cases red**, so a correct fix would look like a regression and the tests would argue for keeping the bug. This is the exact failure mode the project already had a written rule against (finding in row 8 of the log: "a test calibrated to what the build currently does can only ever pass"). | Oracle re-derived from the requirement, which the implementation itself states at `server.js:344`: `// Lẽ ra phải là: if (order.status !== 'pending' && order.status !== 'confirmed')`. TC-14 now expects **2** buttons and is expected to fail; TC-11 asserts label and colour only and declares its omission in a machine-checked `cancel_visibility_owned_by` field naming TC-14/TC-26/TC-27; the phrase "the three cancellable statuses" — the build's vocabulary, not the requirement's — was removed from the titles. TC-14 joins the cancel-guard root-cause cluster, taking the predicted failures from 11 to 12. | The AI derived these two rows from the **probe output** rather than from the requirement: the probe had counted 14 buttons across 16 orders and reported the button absent only on `delivered`/`canceled`, and that observation was written straight into the data file as the expectation. It had even read the developer comment naming the correct rule — it is quoted in TC-26's own note — and still did not notice that TC-11/TC-14 contradicted TC-26. **The failure is not ignorance of the requirement but the absence of a consistency check across rows**: each row was authored and justified in isolation, so no step ever asked "do any two of these forty expectations disagree?" Verifying against the live SUT, the practice introduced to stop the AI guessing, quietly became a source of wrong expected values once it was used to *derive* oracles instead of to confirm locators. |
| 28 | FR-11 | `fr11.order-history.spec.ts`, `order_cancel` branch | Integrity check gated on `if (… && row.cancel_target_status)`, reading state via `myOrders(request, ownerToken)` — with a comment claiming it read "through the owning account" | **The one control case in the cluster asserted nothing.** TC-28 (a user must not cancel another user's order) carried no `cancel_target_status`, so the guard short-circuited and the mutation check was skipped entirely: the case passed on the 404 alone, never verifying that `other`'s order still existed unchanged. And had it run, it would have queried with the **owner's** token — `my-orders` filters on `user_id`, so the lookup returns nothing and `after?.status` is `undefined`, which would then have been compared against an expectation. The comment described behaviour the code did not have. | TC-28 now carries `cancel_target_status: "pending"`; the reader token is chosen by ownership (`targetIsOther ? await ensureOther() : ownerToken`); an explicit `expect(after).toBeDefined()` with the message "the fixture is wrong, not the SUT" fires if the order is invisible to its own owner; and a new conditional data guard **rejects the file** if any refusal-expecting row omits the status it should still hold. | The AI wrote a defensive `&& row.cancel_target_status` so the branch would not crash on rows lacking the field, which converted a missing expectation into a skipped assertion — the safe-looking choice quietly deleted the check. It also wrote a comment stating the correct intent ("through the owning account") while the code used the single token that was already in scope; **the comment was written from the design and never re-read against the line below it**, so a reviewer scanning for exactly this problem would be reassured by the prose. Optional fields plus fail-soft branches is how a data-driven suite grows silent holes. |
| 29 | FR-11 | `fr11.order-history.spec.ts`, `date_valid` branch | `expect(Number.isFinite(Date.parse(rendered))).toBe(true)` on the text of the date cell | **Cross-browser false fail/pass generator.** The cell holds a *localized* string from `toLocaleDateString()`, and the assertion parsed it with Node's `Date.parse`. `10/08/2026` is read as 10 August in en-US order regardless of which the engine meant, so the check can fail on a correct date or pass on a wrong one, and can do either differently on Chromium, Firefox and WebKit. The case existed to catch `Invalid Date` and instead introduced a locale bug of its own — into the very row flagged as "the case most likely to differ across the three engines". | Split into what each layer can actually claim: pattern 1 asserts the cell is non-empty and does not contain `Invalid`; pattern 3 fetches the order's `created_at` from the API, converts it to year/month/day **inside the browser** (whose timezone did the rendering), and requires the cell to contain each part, accepting padded or unpadded forms. Order-agnostic, so `dd/MM/yyyy` and `M/d/yyyy` both pass while a genuinely wrong date does not. | The AI had already written the correct principle in the same file — "the rendered format is the engine's, not the app's, so asserting one would fail on 2 of the 3 required browsers" — and then broke it one line later. It avoided hardcoding a *format* string, which was the hazard it had consciously identified, and did not notice that **parsing** a localized string embeds the same locale assumption less visibly. Having named a risk once, it treated that risk as handled. |
| 30 | FR-11 | `fr11.order-history.spec.ts`, `requireFields()` | The data-file guard called from inside the test body; `money_render` requiring only `seed_owner_orders` | **The guard did not cover the case it was written for, and one branch could assert nothing.** Called from the body, it never ran during `--list`, so a malformed data file still collected cleanly — the guard was absent from the exact check the AI was using as its proof of correctness (same blind spot as finding 26). Separately, `money_render` required neither `expect_money_text` nor `expect_money_not`, so a row with neither would execute two skipped `if`s and **pass while asserting nothing about the money cell**. | `validateAllCases()` now runs at module load, so collection fails on a bad file; required array fields must be **non-empty**; a `CONDITIONAL` table encodes the rules that only apply to some rows (money needs ≥1 expectation; a refusal row must state the status it should still hold; a `status_badge` row must either assert cancel visibility or name the cases that own it); and `FILE_RULES` checks whole-file properties (unique ids, every case has a note, at least one `cancel_click` asserts the dialog, no `cancel_visibility_owned_by` names a nonexistent case). Proven by breaking rows on purpose and restoring: removing TC-32's only money expectation and pointing TC-11's opt-out at a nonexistent `FR11-TC-99` each failed collection with the intended message. | The AI built the guard as a *test-time* precondition because that is where the row's data is used, without asking when the guard would need to speak. It also enumerated required fields per branch mechanically, listing what the branch *reads* rather than what the branch needs in order to **assert** something — so a field whose absence disables an assertion was classified as optional. A guard verified only by the code path it protects, rather than by a deliberate broken input, is an assumption; the difference showed the moment a real broken row was fed to it. |
| 31 | FR-11 | `fr11.order-history.spec.ts`, all five API branches | Hard `expectStatusAmong(...)` placed **before** the mutation/disclosure checks | **The most serious evidence would never have been collected.** A hard assertion throws, so on the 6 rows predicted to answer `200` where a refusal is required, the test would abort at the status line and the report would say only "expected 403, got 200" — never reaching the checks for whether the order record was disclosed, whether the status was written anyway, or whether the rejected checkout created a row. The severity of these findings lives in the second fact, not the first. | Evidence is now read **before** the status assertion, and every API status assertion is `expect.soft` (a `soft` option added to `expectStatusAmong`), so both statements land in the same report; soft failures still fail the test, so nothing is weakened. Extended while there: `admin_orders_list` now reports how many other users' orders were served, and `checkout` verifies a rejected total did not create an order. | This is **finding 3 recurring**, in a different feature, four days later. The AI had already made exactly this correction for FR-01 — the note there reads "it optimised for narrative order, not for what a failing run must still be able to tell you" — and then wrote the natural order again here: assert the response, then inspect the consequences. The earlier fix was recorded as a per-case repair rather than as a rule about assertion ordering, so nothing carried it into new code. **A correction logged as an incident does not generalise; the same mistake will return until it is written as a constraint the next file has to satisfy.** |
| 32 | FR-11 | `fr11_order_history.json`, TC-16 | `expect_sorted_desc_by: "created_at"`, consumed by `expectSortedDescByDate(await columnValues(1))` | **Asserts ordering on a column that cannot express it.** The date cell is `toLocaleDateString()` over a second-granularity `created_at`, so the time is dropped: 16 orders seeded in one run rendered exactly **1** distinct date string. Sorting a one-value-repeated column is vacuous, and combined with finding 10 (which reduced the column to a single element) the assertion could not have failed under any ordering. | `expect_sorted_desc_by: "order_id"`, asserted with the new `expectSortedDescByNumber` over the Mã ĐH column, which is what the backend actually orders by (`ORDER BY id DESC`). The seeded ids are additionally checked to be newest-first among themselves, so the case still means something on a table that has accumulated 200 rows. | The AI chose the column from the *requirement's wording* — FR §7 asks about "newest-first", and `created_at` is the field that means recency — without asking whether the rendered cell preserves enough information to witness it. Choosing what to assert on requires knowing what the UI throws away, and that is a property of the renderer, not of the requirement. |
| 33 | FR-11 | `fr11.order-history.spec.ts`, unauthenticated branch | `await expect(page).toHaveURL(/login\|403\|404/)` | **Cannot hold: `/profile` does not redirect.** Probed on all three engines: an anonymous visit stays at `http://localhost:5173/profile` and renders `Vui lòng đăng nhập` — `Profile.jsx` guards with `if (!user) return <div>…</div>`, not with a navigation. The case would have failed for the wrong reason and reported an access-control problem where there was none. | The `not_logged_in` block asserts the app's own message **and** that no table exists at all, so a build that leaked an empty table to an anonymous visitor cannot pass on the message alone. | Finding 8's pattern a third time: the AI assumed refusal is expressed as a route change, which is the default for a routed SPA, and this page expresses it as a conditional render. Three findings (8, 33, and FR-13's 17) now share it, which is why it is stated as a rule rather than a repair: **on this SUT, never assert refusal as a URL — assert the rendered consequence.** |

Finding 34 comes from the first attempted execution, before any FR-11 assertion ran.

| # | Feature | File / line | What the AI produced | Why it is wrong | What I changed it to | Why the AI missed it |
|---|---|---|---|---|---|---|
| 34 | FR-11 | `fr11.order-history.spec.ts`, identity setup | `api.login(owner)` and `api.login(admin)` inside every test, plus UI form login for each authenticated UI row | **Execution blocker — all 40 cases failed in setup with HTTP 429 on all three engines.** The backend applies one limiter to the entire `/api` surface: 200 requests / 15 minutes / IP. The suite spent 80 requests per browser only reacquiring the same two tokens, before its 71 order-seeding calls and the assertions' own traffic. The reports therefore contained 40 identical setup failures and zero evidence about FR-11. | Owner/admin plus the reusable `other` and `empty` identities authenticate once in Playwright `globalSetup`; replacement workers read the same tokens from a mode-0600 OS-temporary cache, and `globalTeardown` removes it. UI cases stage localStorage with `addInitScript` before `/profile` instead of exercising the unrelated login form or opening an extra home page. The limiter remains enabled — no `LOADTEST=1` bypass — and type-check/collection still pass. | The AI reviewed each test for cross-run data collisions but never budgeted the suite's **aggregate request count** against a global SUT constraint it had already read during environment setup. Per-test isolation was treated as automatically safer; here it multiplied an immutable setup action by 40 and made the feature unexecutable. This is the API analogue of findings 5/6/11: a property of the full run is invisible when code is assessed one case at a time. |
| 35 | FR-11 | `fr11.order-history.spec.ts`, first rate-limit correction | The first correction moved authentication into `test.beforeAll` and assumed it would execute once per browser run | **Still an execution blocker after real product failures began.** Playwright discards a worker after a failed test and creates a replacement worker; that worker runs `beforeAll` again. The first corrected Chromium run reached seven genuine red cases, then repeated authentication enough times to receive HTTP 429 at TC-24; 21 of 46 cases never ran. | Authentication now happens in `globalSetup`, above worker lifetime. A deliberately external temporary cache supplies the four tokens to every replacement worker, while stale state is removed both before setup and in `globalTeardown`. This preserves failure isolation without consuming another login request after each red test. | The AI reasoned about hook scope from the source layout (one `describe`, one `beforeAll`) rather than from Playwright's failure lifecycle. Static type-check and `--list` cannot expose worker replacement, so the proposed repair was accepted without the one check that mattered: continuing a run through several intentionally failing tests. |
| 36 | FR-11 | execution schedule, 46-case run | After removing repeated login traffic, the AI assumed one full run would fit below the SUT's 200-request limit | **The final three cases were still contaminated by HTTP 429.** Forty-six business cases legitimately perform more than 200 setup, UI and verification requests even when authentication is optimal. Running them as one process therefore cannot produce complete evidence while the production limiter remains enabled. Playwright `--shard=1/2` also did not help because all cases are one serial test group in one spec file, so shard 1 executed all 46. | Execute two explicit ID groups (26 and 20 cases), restart the disposable SUT backend between groups, write each to Playwright blob evidence, then merge the blobs into one HTML/JSON report. The limiter is never disabled and no case or oracle is changed. | The AI optimized repeated requests but did not measure the irreducible request budget of the full expanded suite. It also assumed sharding distributed individual tests, overlooking that serial tests in one file remain one indivisible test group. Both errors came from treating runner features as labels instead of verifying their actual scheduling semantics. |
| 37 | FR-11 | `fr11.order-history.spec.ts`, TC-45 | A shared line evaluated `row.expect_http_any_of!.includes(200)` before switching on `api_call`, although the race case deliberately uses `expect_http_multiset` instead | **TC-45 crashed in test code before sending the two simultaneous cancel requests.** The data validator correctly required the multiset for `order_cancel_race`, but the shared pre-switch computation imposed an unrelated optional field at runtime. | The shared denial flag is now conditional on `expect_http_any_of` being present. Branch-specific validation still makes that field mandatory everywhere it is consumed; TC-45 retains its exact `[200, 400]` multiset oracle and final-state assertion. | The AI validated fields per branch but placed a branch-specific derived value above the branch dispatch. Non-null assertion syntax suppressed the compiler warning, so type-check and collection passed; only executing the new branch exposed the contradiction between the validator and shared control flow. |
| 38 | FR-11 | `fr11.order-history.spec.ts`, TC-26 `cancel_click` | Click the shipping-order cancel button and immediately assert that its badge still says `Đang giao` | **A false pass on Chromium and Firefox.** The badge already held the expected pre-action value, so Playwright's web-first assertion succeeded immediately before the cancel request completed and the UI refreshed. Faster WebKit exposed the real outcome (`Đã hủy` plus the success dialog), proving the prior green results had asserted the starting state rather than the postcondition. | Register waits for both the matching PUT `/api/orders/:id/cancel` response and the subsequent GET `/api/orders/my-orders` refresh before checking the badge. The oracle remains `Đang giao`; only the observation point moves after the complete business action. | The AI used a web-first assertion and treated that as sufficient synchronization. Polling helps only when the initial value is wrong and must become right; here the initial value was already the expected value, so auto-waiting made the race invisible. Cross-browser execution was the first check capable of exposing it. |
| 39 | FR-11 | `fr11.order-history.spec.ts`, first TC-26 synchronization repair | The new response predicate assumed cancel used POST | **The repair timed out even though the order visibly became canceled.** `Profile.jsx` calls `axios.put`, so the waiter deliberately ignored the exact response it needed; the screenshot showed `Đã hủy` while the report misleadingly foregrounded a 15-second wait timeout. | Match the verified PUT `/api/orders/:id/cancel` response, then the GET history refresh. | The endpoint path was known, so the HTTP verb was filled from REST convention instead of re-reading the client code. A synchronization repair is only as reliable as every part of its event predicate; guessing one field converts a real product failure into a test timeout. |

### Categories the brief names explicitly

Use these as prompts for your own review pass — but only record what you actually found.

- **Fragile selectors** — CSS/XPath tied to generated class names, nth-child chains,
  text matching that breaks under the SUT's Vietnamese UI.
- **Weak or missing assertions** — asserting an element exists rather than that it
  says the right thing; asserting a success toast without checking the server agreed.
- **Missing edge cases** — boundary values the AI skipped because they were not in
  the prompt; the AI tends to generate the happy path plus one obvious negative.
- **Flaky waits** — `waitForTimeout`, arbitrary sleeps, or racing an animation
  instead of using a web-first assertion.
- **Wrong-by-construction data** — hardcoded emails that collide on the second run,
  fixtures that mutate shared state so run 2 of 9 behaves differently from run 1.

## Cross-browser differences observed

Nine runs across three engines will not behave identically. Record real differences
here; do not file browser-engine differences as SUT bugs unless the SUT is genuinely
broken on that engine.

| Feature | Browser | Difference observed | SUT defect or engine behaviour? | Evidence |
|---|---|---|---|---|
| FR-01 | firefox | Run 2 (2026-08-10, after the TC-01b fix): **TC-01a failed on firefox only** — 16/14, against 17/13 on chromium and webkit. The one recorded error is `browserContext.close: Protocol error (Browser.removeBrowserContext): can't access property "_maybeDontRestoreTabs", this._windows[aWindow.__SSi] is undefined`, raised during context teardown; no assertion error is recorded for the test. The same case passed on chromium and webkit in run 2 and on all three engines in run 1. | _<your call — the raw facts are in the evidence column; not filled in by the AI>_ | `reports/final/json/fr01-firefox.json` (TC-01a, 1 error, no failed assertion); run 1 comparison in `reports/history/archived-runs/fr01_run1_2026-08-10/` |
| FR-01 | all three | The 13 specification divergences behave identically on chromium, firefox and webkit — same assertions, same received values. No engine-specific SUT behaviour was observed in this feature. | Engine-independent | `reports/final/json/fr01-*.json`, both runs |

## Cases that could not be automated

Aggregated from the three TC matrices, with reasons.

| TC ID | Feature | Why not automatable | Who/what could run it |
|---|---|---|---|
| TC-17 | FR-01 | "Confirm ≠ Password" needs a Confirm Password field to type a differing value into. `Register.jsx` renders no such field, and the API body `{name, email, password}` has no such key — unreachable at both interfaces. | Nobody, on this build. Testable only if the field is added; worth raising as a requirements-vs-implementation gap. |
| TC-23 | FR-01 | "Confirm Password empty" — same missing field. | Same as TC-17. |
| EC-34 (no TC) | FR-01 | Designed as a *deviant* condition ("Confirm Password absent"), but on this build that is the page's **normal** state, so it discriminates nothing. | — (moot rather than deferred) |
| TC-05 (partial) | FR-01 | The status code is asserted, but whether a row was written cannot be observed: the request omitted `email`, and `/api/login` is keyed on e-mail. | Direct sqlite inspection, or an admin list endpoint. Recorded as a `Coverage gap` annotation in the run report rather than covered by a weaker check. |
| TC-11 (partial) | FR-01 | Same, for the row that omits `password`: `/api/login` verifies the password, so a row created with none cannot be found through it. | Same as TC-05. |
